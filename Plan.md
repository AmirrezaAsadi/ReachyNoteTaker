You are setting up a fully local voice-powered note-taking tool on an Apple 
Silicon Mac (M1 Max, 64GB unified memory).

Create a repo called `voice-notes-local` with the following structure and 
all necessary files:

## Goal
A local speech-to-text note-taking tool using:
- VAD: Silero VAD v5 (CPU) — detects when user starts/stops speaking
- STT: Parakeet-TDT 0.6B v3 (via parakeet-mlx, Apple Silicon optimized)
- LLM: Gemma 4 E4B served via llama.cpp — cleans, structures, and 
  summarizes notes
- TTS: Qwen3-TTS 1.7B (via mlx-audio) — optional readback of notes
- Pipeline glue: huggingface/speech-to-speech library

## Core Features (Phase 1 — Standalone Mac tool)
- Speak freely, notes are transcribed in real time
- LLM automatically:
  - Cleans up filler words (um, uh, like)
  - Fixes punctuation and capitalization
  - Detects structure (headings, bullet points, action items, dates)
  - Generates a short summary at the end of each session
- Notes saved as timestamped markdown files
- Optional TTS readback: "Here's what I captured..." after each pause
- Voice commands:
  - "New note" — starts a fresh note
  - "Save note" — saves current note to file
  - "Read back" — TTS reads the current note aloud
  - "Summarize" — LLM generates a summary of the session
  - "Add tag [tagname]" — tags the current note
  - "Cancel" — discards unsaved note
- Sessions organized by date in ~/voice-notes/
- Each note saved as markdown with frontmatter:
  - title (auto-generated by LLM)
  - date
  - tags
  - summary
  - duration
  - word count

## Note Storage Structure
~/voice-notes/
├── 2026-06-02/
│   ├── note-001-meeting-recap.md
│   ├── note-002-ideas.md
│   └── session-summary.md
├── tags/
│   └── index.json  (tag → file mapping)
└── search-index.json

## Requirements

1. Create `README.md` with:
   - What this is and what it does
   - Prerequisites (Homebrew, Python 3.11+, uv)
   - Install instructions
   - How to start a note-taking session
   - All voice commands with examples
   - How to search notes (CLI)
   - How to configure custom TTS voice
   - Phase 2 preview section: "Coming soon — Reachy Mini integration"
     explaining how the robot will serve as a physical note-taking 
     companion (always-on listening, visual feedback, speaks summaries)
   - Troubleshooting section

2. Create `setup.sh` that:
   - Checks for Homebrew, installs if missing
   - Installs llama.cpp via brew
   - Installs uv if missing
   - Creates Python 3.11 virtual environment
   - Installs all Python dependencies
   - Downloads Gemma 4 E4B GGUF model
   - Creates ~/voice-notes/ directory structure
   - Prints success message and quick-start instructions

3. Create `requirements.txt` with pinned versions for:
   - speech-to-speech (huggingface)
   - parakeet-mlx
   - mlx-audio
   - mlx-lm
   - silero-vad
   - torch (MPS-compatible)
   - torchaudio
   - sounddevice
   - pynput (for hotkey support)
   - watchdog (for file monitoring)
   - rich (for beautiful terminal UI)
   - click (for CLI commands)
   - numpy
   - Any other required dependencies

4. Create `note_taker.py` — the main app:
   - Rich terminal UI showing:
     - Live transcription as you speak
     - Current note being built
     - Detected structure (bullets, headings, action items highlighted)
     - Status bar: recording/processing/idle
     - Session stats: duration, word count, notes saved
   - VAD-driven recording loop (auto-detects speech start/stop)
   - Voice command detection runs before sending to note LLM
   - After each speech segment, LLM cleans and structures the text
   - After silence > 30 seconds, auto-saves current note
   - Graceful shutdown on Ctrl+C with auto-save

5. Create `llm_processor.py`:
   - `clean_transcript(raw_text)` — removes fillers, fixes punctuation
   - `structure_note(text)` — detects and formats bullets, headings, 
     action items (prefixed with "TODO:")
   - `generate_title(text)` — creates a short descriptive title
   - `summarize_session(notes_list)` — end-of-session summary
   - `detect_voice_command(text)` — returns command name or None
   - All functions call local llama-server via OpenAI-compatible API
   - Include clear system prompts for each function

6. Create `note_store.py`:
   - `save_note(content, title, tags)` — saves to dated folder as markdown
   - `load_note(filepath)` — loads and parses a note
   - `search_notes(query)` — full-text search across all notes
   - `list_notes(date=None, tag=None)` — lists notes with filters
   - `get_session_summary(date)` — loads session summary for a date
   - `update_tag_index(filepath, tags)` — maintains tags/index.json
   - `build_search_index()` — rebuilds search-index.json

7. Create `tts_reader.py`:
   - `read_text(text)` — speaks text via Qwen3-TTS
   - `read_note(filepath)` — reads a saved note aloud
   - `confirm(message)` — short spoken confirmations 
     ("Got it", "Note saved", "Starting new note")
   - Uses voice preset if one exists, falls back to default
   - Can be disabled via .env (TTS_ENABLED=false) for silent mode

8. Create `run.sh` that:
   - Starts llama-server with Gemma 4 E4B in background
   - Waits for server health check
   - Starts note_taker.py
   - Handles clean shutdown on Ctrl+C (saves any open note first)

9. Create `search.sh` CLI tool:
   - `./search.sh "meeting"` — searches all notes
   - `./search.sh --tag work` — filter by tag
   - `./search.sh --date 2026-06-02` — filter by date
   - `./search.sh --today` — show today's notes
   - Pretty prints results with Rich

10. Create `clone_voice.sh`:
    - Takes a .wav file as argument (5–15 seconds)
    - Validates file length
    - Creates voice preset via Qwen3-TTS cloning
    - Saves preset to ~/.voice-notes-voice.qvp
    - Usage: `./clone_voice.sh my_voice.wav`

11. Create `test_pipeline.py`:
    - Tests each component loads (VAD, STT, LLM, TTS)
    - Runs a mock note-taking session with a sample audio clip
    - Tests each LLM processor function with sample text
    - Tests note save/load/search cycle
    - Reports latency per stage and memory per component
    - Pass/fail for each test

12. Create `.env.example`:
    - LLM_MODEL_PATH
    - LLM_CONTEXT_SIZE=8192
    - STT_LANGUAGE=en
    - TTS_ENABLED=true
    - TTS_VOICE_PRESET=~/.voice-notes-voice.qvp
    - NOTES_DIR=~/voice-notes
    - AUTO_SAVE_SILENCE_SECONDS=30
    - SERVER_PORT=8080
    - LOG_LEVEL=INFO
    - PHASE2_REACHY_MINI=false  ← placeholder for Phase 2

13. Create `PHASE2_ROADMAP.md`:

    ## Vision
    Reachy Mini as a physical note-taking companion. You speak, 
    it listens, reacts, and captures — like a attentive colleague 
    sitting across from you.

    ## Robot Behavior States
    Reachy Mini has distinct physical behaviors for each pipeline stage.
    Create `reachy_gestures.py` that defines and manages these states:

    ### State 1: IDLE (waiting for speech)
    - Head slowly and gently sways side to side, very subtle
    - Eyes dim slightly (if controllable)
    - Neutral resting expression
    - Behavior: "I'm here, ready when you are"

    ### State 2: LISTENING (VAD detected speech)
    - Head orients and tilts slightly toward the speaker (attentive lean)
    - Eyes brighten
    - Very slight rhythmic nod in sync with speech energy/volume
      (louder speech = slightly more pronounced nod)
    - Behavior: "I hear you, keep going"

    ### State 3: PROCESSING (STT + LLM cleaning the note)
    - Head does a slow thoughtful tilt to one side
    - Small repetitive micro-nod, like thinking/considering
    - Slightly faster than idle sway but more purposeful
    - Behavior: "I'm thinking about what you said"

    ### State 4: WRITING (LLM structuring, saving to file)
    - Head bobs downward rhythmically, like looking down at a notepad
    - Short quick nods, like pen strokes
    - Speed of nodding matches estimated writing speed 
      (longer note = more bobs)
    - Behavior: "I'm writing this down"

    ### State 5: CONFIRMING (note saved, voice command accepted)
    - Single confident slow nod downward then back up
    - Brief pause, then returns to IDLE
    - Behavior: "Got it, done"

    ### State 6: READING BACK (TTS speaking the note)
    - Head moves gently with speech rhythm (TTS prosody-driven)
    - Slight left-right scanning motion as if reading lines on a page
    - Behavior: "Let me read that back to you"

    ### State 7: SUMMARIZING (end of session)
    - Head lifts upward slightly (looking up, reflective)
    - Slow gentle nodding while summary is being generated
    - Then transitions to READING BACK for the spoken summary
    - Behavior: "Let me think about everything we covered"

    ### State 8: ERROR / DIDN'T CATCH THAT
    - Head tilts to one side with a slight confused lean
    - Stays in that position briefly
    - Short double-shake (not aggressive, more quizzical)
    - Behavior: "Hmm, I didn't quite get that"

    ## Implementation Plan for `reachy_gestures.py`
    - `set_state(state: RobotState)` — transitions robot to a behavior state
    - `sync_to_audio_energy(energy_level: float)` — adjusts nod intensity
      to live microphone energy during LISTENING state
    - `sync_to_note_length(word_count: int)` — controls bob count 
      during WRITING state based on note length
    - `sync_to_tts_prosody(phoneme_timestamps)` — drives head motion
      during READING BACK from TTS output timing
    - All motions are non-blocking (run in background thread)
    - Smooth transitions between states (no snapping)
    - Motion parameters tunable via .env:
      - GESTURE_INTENSITY=0.5 (0.0 = still, 1.0 = expressive)
      - GESTURE_SPEED=1.0 (multiplier)
      - GESTURE_ENABLED=true

    ## What Changes in the Codebase for Phase 2
    - `note_taker.py` gains a --robot flag that activates gesture sync
    - `llm_processor.py` emits state change events the gesture 
      system subscribes to
    - `tts_reader.py` passes phoneme timing to gesture system 
      during readback
    - VAD energy level streamed to gesture system during LISTENING
    - `reachy_gestures.py` wraps the Reachy Mini Python SDK
    - note_taker.py becomes a WebSocket server, Reachy is the client
    - Wake word added: "Hey Reachy, take a note"

    ## Gesture Tuning Notes
    - WRITING state bob speed should feel like natural handwriting pace
      (~1 bob per 3–4 words estimated)
    - LISTENING nod should never feel like aggressive agreement,
      keep it subtle — more like active listening than enthusiasm
    - PROCESSING tilt should go to the same side every time 
      (consistent = feels like personality, not random)
    - All motions must stop cleanly if robot loses connection
    - Never run two motion states simultaneously — queue them

    ## Estimated Effort per Component
    - reachy_gestures.py core:         ~2 days
    - VAD energy → nod sync:           ~1 day
    - TTS prosody → head motion sync:  ~2 days
    - State machine + transitions:     ~1 day
    - Tuning and feel:                 ~2 days (most important part)
    - WebSocket server mode:           ~1 day
    - Wake word integration:           ~1 day
    - Total estimate:                  ~10 days

    ## References
    - Reachy Mini Python SDK: pollen-robotics/reachy_mini_conversation_app
    - Reachy Mini joint control docs
    - huggingface/speech-to-speech WebSocket server mode

